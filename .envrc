# Auto-activate the Nix devShell on entering this folder (nix-direnv caches it,
# so re-entry is instant unless flake.nix/flake.lock change). Run `direnv allow`
# once after editing. `.direnv/` is gitignored.
use flake
